
Eventlistener service is generated by the el itself
% oc expose service X --name=y --hostname=<webhook string> ##match the http or https

For the build pipeline

1. Create a .configjson secret and use in the docker-config (bootc-image-registry-creds)
2. Reference the pipeline SA and give it pull, mount permissions

 oc describe sa pipeline
Name:                pipeline
Namespace:           bootc-image-build
Labels:              <none>
Annotations:         openshift.io/internal-registry-pull-secret-ref: pipeline-dockercfg-pnxnh
Image pull secrets:  pipeline-dockercfg-pnxnh
                     bootc-image-docker-config
                     quay-io-push-secret
Mountable secrets:   pipeline-dockercfg-pnxnh
                     bootc-image-docker-config
                     quay-io-push-secret
Tokens:              <none>
Events:              <none>

